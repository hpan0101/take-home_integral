# Copy this file to .env or .env.local and update the values as needed

# Database URL - SQLite database file location
DATABASE_URL="file:./dev.db"

# NextAuth.js (required for session encryption). Generate with: openssl rand -base64 32
AUTH_SECRET="your-secret-at-least-32-chars-long"
